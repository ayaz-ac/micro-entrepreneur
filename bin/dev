#!/usr/bin/env sh

if ! command -v overmind > /dev/null 2>&1; then
  echo "Overmind is not installed. Install it with:"
  echo "  brew install overmind (macOS)"
  echo "  Or visit: https://github.com/DarthSim/overmind"
  exit 1
fi

exec overmind start -f Procfile.dev "$@"
