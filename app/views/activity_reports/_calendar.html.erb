<div class="w-full grid grid-cols-7 bg-neutral-100 gap-px border-b border-x border-brand-500">
  <div class="col-span-7 grid grid-cols-7 bg-off-white place-items-center lg:place-items-start px-2 lg:px-6 py-4 lg:border lg:border-brand-300">
    <% Date::DAYNAMES.rotate.each do |weekday| %>
      <div class="text-black text-xs lg:text-lg">
        <%= t('date.day_names')[Date::DAYNAMES.rotate.index(weekday)] %>
      </div>
    <% end %>
  </div>
  <% month_offset(activity_report.start_date).times do %>
    <div class="h-24 bg-brand-100"></div>
  <% end %>
  <% (activity_report.days).each do |day| %>
    <%= render 'activity_reports/day', day:, activity_report: activity_report %>
  <% end %>
  <% month_inset(activity_report.start_date).times do %>
    <div class="h-24 bg-brand-50"></div>
  <% end %>
</div>
