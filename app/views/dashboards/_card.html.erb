<div class="w-full flex flex-col space-y-1.5 rounded-xl <%= status == 'alert' ? 'bg-red-200' : 'bg-white' %> px-6 py-4 lg:grow">
  <%= yield %>
  <span class="text-4xl font-bold text-brand-950"><%= value %></span>
  <div class="flex flex-col space-y-0.5">
    <span class="text-lg"><%= title %></span>
    <span class="text-sm"><%= subtitle %></span>
  </div>
</div>
