<div class="flex flex-col items-center w-full px-6 lg:px-44 mt-12 lg:mt-3">
  <h2 class="text-gray-900 text-center text-2xl lg:text-5xl lg:leading-normal font-semibold">Mon profil</h2>
  <%= form_with model: current_user, url: user_path(current_user), builder: TailwindBuilder, class: 'w-full lg:w-2/5 mt-3' do |f| %>
    <div class="mt-7 flex items-center space-x-6">
      <%= f.label :average_daily_rate %>
      <%= f.number_field :average_daily_rate %>
    </div>
    <div class="mt-8 flex justify-center">
      <%= f.submit 'Mettre à jour' %>
    </div>
  <% end %>
</div>
